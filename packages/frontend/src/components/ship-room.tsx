import React from 'react';

// Ship room component removed - focusing on Admin functionality only
// If ship room functionality is needed later, it should use direct API calls instead of LiveStore

export const ShipRoom: React.FC = () => {
	return (
		<g>
			<text x="50" y="50" fill="white" fontSize="12" textAnchor="middle">
				Ship Room Unavailable
			</text>
		</g>
	);
};
